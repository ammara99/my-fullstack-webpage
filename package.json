{
  "name": "my-fullstack-webpage",
  "version": "1.0.0",
  "description": "A simple full-stack webpage",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",  // This will start the app in production
    "build": "yarn install"     // This will install dependencies using Yarn
  },
  "dependencies": {
    "axios": "^0.24.0",         // For making API calls
    "express": "^4.17.1",       // Web framework for Node.js
    "dotenv": "^10.0.0"         // To load environment variables from .env file
  },
  "devDependencies": {
    "nodemon": "^2.0.15"         // For automatic server reload in development
  },
  "engines": {
    "node": ">=14.0.0"           // Ensure Node.js version compatibility
  },
  "license": "ISC"
}