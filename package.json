{
  "name": "simple-webpage",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",      // Start the server using Node.js
    "dev": "nodemon server.js"      // Optional for local development
  },
  "dependencies": {
    "express": "^4.17.1",           // Express framework to handle routes
    "dotenv": "^10.0.0"             // Optional: for loading environment variables from a .env file
  },
  "engines": {
    "node": "14.x"                  // Optional: specify the Node.js version
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/yourusername/simple-webpage.git"
  }
}
